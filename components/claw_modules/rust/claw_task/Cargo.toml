[package]
name = "claw_task"
version = "0.1.0"
edition = "2021"
description = "FreeRTOS task stack-policy logic (pure Rust); the claw_task_* C ABI lives in claw_capi"

[lib]
crate-type = ["rlib"]

[dependencies]
