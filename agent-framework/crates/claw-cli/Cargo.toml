[package]
name = "claw-cli"
authors = ["Finn(Ziheng) Sheng <zsheng2@ncsu.edu>", "Finn(Ziheng) Sheng <robcholz00@gmail.com>"]
version = "0.1.0"
edition = "2021"
description = "Host CLI applications for the ESP-Claw agent framework"

[[bin]]
name = "claw-agent-chat"
path = "src/main.rs"

[dependencies]
claw-agent = { path = "../claw-agent", default-features = false, features=["cache_profile", "multiagent", "intrusive-observability"] }
claw-interface = { path = "../claw-interface", features = ["host-backends"] }
claw-log = { path = "../claw-log" }
anyhow = "1"
dotenvy = "0.15"
tokio = { version = "1.52.3", features = ["macros", "rt-multi-thread", "sync", "time"] }
futures-lite = "2"
anstyle = "1"
rustyline = { version = "18.0.1", default-features = false }
strum = { version = "0.28", features = ["derive"] }
terminal_size = "0.4.4"
unicode-width = "0.2.2"
thiserror = "2"
tracing = { version = "0.1", default-features = false, features = ["std"] }

[dev-dependencies]
tempdir = "0.3"
