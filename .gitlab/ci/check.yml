check_framework:
  stage: check
  tags:
    - build
  image: rust:latest
  extends:
    - .rules:check:framework
  script:
    - rustup component add --toolchain stable rustfmt clippy
    - rustup toolchain install nightly --profile minimal
    - cargo +stable install cargo-public-api --locked
    - ./framework/check.sh
    - ./framework/harness.sh
