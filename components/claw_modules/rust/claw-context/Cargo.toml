[package]
name = "claw-context"
authors = ["Finn(Ziheng) Sheng"]
version = "0.1.0"
edition = "2021"
description = "Context builder: assembles the LLM request context from memory, history, skills, and other providers"

[lib]
name = "claw_context"
# Pure-Rust rlib bundled (with the other logic crates) into the single `claw_rt`
# staticlib that the firmware links.
crate-type = ["rlib"]

[dependencies]

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
unwrap_used = "forbid"
expect_used = "forbid"
indexing_slicing = "forbid"
panic = "forbid"
todo = "warn"
unimplemented = "warn"
unreachable = "warn"
arithmetic_side_effects = "forbid"
