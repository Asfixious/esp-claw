[package]
name = "claw_paths"
version = "0.1.0"
edition = "2021"
description = "Boot-time logical storage root registry (pure Rust; the claw_paths_* C ABI lives in claw_capi)"

[lib]
crate-type = ["rlib"]

[dependencies]
