The child does not see the parent conversation; make `goal` complete and
standalone.

For multi-step work involving LLM calls, tools, network access, or device I/O,
set `timeout_ms` to at least 300000.
