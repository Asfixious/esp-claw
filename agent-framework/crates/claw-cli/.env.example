# Copy this file to `.env.local` and fill in values for `claw-agent-chat`.
#
#   cp .env.example .env.local
#
# Unit tests do not need these values. The CLI fails clearly when a required
# value is missing.

CLAW_LLM_API_KEY=
CLAW_LLM_BASE_URL=https://api.openai.com
CLAW_LLM_MODEL=gpt-4o-mini
