{
  "type": "function",
  "function": {
    "name": "begin_execution",
    "description": "Confirm the request is feasible and begin executing it (this unlocks delegating work to subagents). Provide a short plan.",
    "parameters": {
      "type": "object",
      "properties": {
        "plan": {
          "type": "string",
          "description": "A short plan of how the task will be executed."
        }
      },
      "required": ["plan"]
    }
  }
}
