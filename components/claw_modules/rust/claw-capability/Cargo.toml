[package]
name = "claw-capability"
authors = ["Finn(Ziheng) Sheng"]
version = "0.1.0"
edition = "2021"
description = "Capability registry and dispatch (Rust-first; C handlers are adapters)"

[lib]
crate-type = ["rlib"]

[dependencies]
thiserror = "2"
serde_json = "1"
