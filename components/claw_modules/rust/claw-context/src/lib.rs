//! `claw-context` — the agent context builder.
//!
//! This crate assembles the context handed to the LLM for each iteration,
//! drawing from memory, session history, skills, and other context providers.
