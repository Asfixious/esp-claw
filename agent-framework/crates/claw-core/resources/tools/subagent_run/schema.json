{
  "type": "function",
  "function": {
    "name": "subagent_run",
    "description": "Run a subagent and wait for its result.",
    "parameters": {
      "type": "object",
      "properties": {
        "kind": {
          "type": "string",
          "description": "Agent kind from subagent_list_spawnable."
        },
        "name": {
          "type": "string",
          "description": "Short label for the subagent."
        },
        "goal": {
          "type": "string",
          "description": "Standalone task for the subagent."
        },
        "timeout_ms": {
          "type": "integer",
          "minimum": 1,
          "default": 300000,
          "description": "Maximum lifetime in milliseconds. Use at least 300000 for multi-step tasks involving LLM calls, tools, network access, or device I/O; 120000 is often too short on embedded targets."
        }
      },
      "required": ["kind", "name", "goal", "timeout_ms"]
    }
  }
}
