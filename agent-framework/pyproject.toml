[project]
name = "agent-framework-tools"
version = "0.0.0"
description = "Python tooling workspace for agent-framework."
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false

[tool.uv.workspace]
members = [
    "bench/llm-tape",
    "crates/claw-log",
    "crates/claw-log/scripts/serial_log",
]
